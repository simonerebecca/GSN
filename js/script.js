// Voorbeeld van dynamische data voor elke sectie
document.addEventListener("DOMContentLoaded", function () {
    // Dynamische updates kunnen hier worden toegevoegd
    const actueelInfo = document.getElementById("actueel");
    actueelInfo.innerHTML += "<p>Het laatste nieuws wordt hier geladen...</p>";
});
